image: registry.sensetime.com/eig-research/pytorch:1.3.1-cuda10.1-cudnn7-devel

stages:
    - linting
    - test

before_script:
  - echo $PATH
  - gcc --version
  - nvcc --version
  - python --version
  - pip --version
  - python -c "import torch; print(torch.__version__)"

linting:
  stage: linting
  script:
    - pip install flake8 yapf isort
    - flake8 .
    - isort -rc --check-only --diff mmedit/ tools/ tests/
    - yapf -r -d mmedit/ tools/ tests/ configs/

test:
  stage: test
  script:
    - echo "Install MMCV"
    - ssh-keyscan gitlab.sz.sensetime.com >> ~/.ssh/known_hosts
    - git clone git@gitlab.sz.sensetime.com:EIG-Research/mmcv.git
    - cd mmcv && git checkout newer-runner && pip install -e . && cd ..
    - echo "Start building..."
    - pip install pillow==6.2.1 lmdb scikit-image yapf
    - pip install -e .
    - python -c "import mmedit; print(mmedit.__version__)"
    - echo "Start testing..."
    - pip install pytest coverage
    - coverage run --branch --source mmedit -m pytest tests/
    - coverage report -m
